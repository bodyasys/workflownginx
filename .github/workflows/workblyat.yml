name: Test SSH

on:
  workflow_dispatch:

jobs:
  ConntctSSH:
    runs-on: self-hosted
    steps:
      - name: takurva
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.KEY }}
          port: ${{ secrets.PORT }}
          script: |
                 whoami
                 ls -al
